using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDataCode : MonoBehaviour
{
    [SerializeField] DataManager DataManager;

    private void Start()
    {
        //Word word = GetWordName("W01");
        //Debug.Log(word.wordID + " : " + word.wordName);
    }
}
